// tests/scripts/chat-smoke.ts
/*
await chatRepo.getOrCreateDirectChannel(u1, u2);
await chatRepo.saveMessage(msg);
const msgs = await chatRepo.listMessages(chan.id, 30);
console.log(msgs.length);
*/
